server {

    listen 80;

    server_tokens off;

    client_max_body_size 10M;

    location /api/ {
        proxy_pass https://kittygram-final-qwfo.onrender.com/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /admin/ {
        proxy_pass https://kittygram-final-qwfo.onrender.com/admin/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /media/ {
        alias /media/;
    }

    location / {
        root /static;
        index index.html index.htm;
        try_files $uri /index.html;
    }
}